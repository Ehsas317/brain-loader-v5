# Brain Loader v5 — Core Dependencies
# Python 3.11+

trio>=0.25.0          # Structured concurrency
httpx>=0.27.0         # Async HTTP for all API providers
pyyaml>=6.0.1         # Config file parsing

# Optional: Install for enhanced experience
# rich>=13.0          # Beautiful terminal UI (future)
